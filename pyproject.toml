[project]
name = "tw-lottery-recommendation"
version = "1.0.5"
description = "Taiwan lottery draw analyzer and number recommender with a Transformer ML model"
readme = "README.md"
requires-python = ">=3.14"
license = { text = "MIT" }
dependencies = [
    "numpy>=2.4.4",
    "requests>=2.33.1",
    "rich>=15.0.0",
    "torch>=2.11.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[project.scripts]
lottery = "cli:main"

[tool.uv]
package = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["scraper", "analyzer", "data", "ml"]
include = ["cli.py"]
